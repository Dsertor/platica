import style from './selectButtonIndo.module.css';

export const SelectButtonInfo = () => {
  return (
    <div className={`${style.textInfo} `}>Selecciona un botón que se encuentra a su izquierda para comenzar con la aplicación</div>
  )
}
